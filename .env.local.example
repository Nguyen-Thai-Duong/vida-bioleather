# Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# MongoDB Connection
MONGODB_URI=your_mongodb_connection_string

# JWT Secret for Authentication
JWT_SECRET=your_jwt_secret_key

# Google Sheets Web App URL
# To set this up:
# 1. Open https://docs.google.com/spreadsheets/d/11PNLDW1USJ0tAuFiYHnTzh1AgY_bm04xYzpJrkwSTYU/edit
# 2. Go to Extensions → Apps Script
# 3. Delete any existing code and paste the script below
# 4. Click Deploy → New deployment → Select type: Web app
# 5. Execute as: Me
# 6. Who has access: Anyone
# 7. Click Deploy and copy the Web app URL
# 8. Paste the URL below
GOOGLE_SHEETS_SCRIPT_URL=your_google_apps_script_web_app_url_here

# Google Apps Script code to paste:
# function doPost(e) {
#   try {
#     const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
#     const data = JSON.parse(e.postData.contents);
#     
#     sheet.appendRow([
#       data.timestamp,
#       data.name,
#       data.email,
#       data.subject,
#       data.message
#     ]);
#     
#     return ContentService.createTextOutput(JSON.stringify({
#       status: 'success',
#       message: 'Data saved successfully'
#     })).setMimeType(ContentService.MimeType.JSON);
#   } catch (error) {
#     return ContentService.createTextOutput(JSON.stringify({
#       status: 'error',
#       message: error.toString()
#     })).setMimeType(ContentService.MimeType.JSON);
#   }
# }
